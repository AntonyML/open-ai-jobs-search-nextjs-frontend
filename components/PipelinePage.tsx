/* eslint-disable @typescript-eslint/no-explicit-any */
'use client'
import { FormEvent,useEffect,useState } from 'react'; import { useRouter } from 'next/navigation'; import { apiFetch } from '@/lib/api'
type Props={title:string; eyebrow:string; endpoint:string; listEndpoint?:string; fields:{name:string;label:string;type?:string;optional?:boolean}[]; step:number; next?:string; actionLabel?:string}
export default function PipelinePage({title,eyebrow,endpoint,listEndpoint,fields,step,next,actionLabel='Run'}:Props){const [form,setForm]=useState<Record<string,string>>({}),[items,setItems]=useState<any[]>([]),[result,setResult]=useState<any>(null),[loading,setLoading]=useState(false),[error,setError]=useState(''),router=useRouter();useEffect(()=>{if(listEndpoint)apiFetch<any>(listEndpoint).then(x=>setItems(Array.isArray(x)?x:(x.items||x.jobs||x.applications||x.rows||[]))).catch(()=>{})},[listEndpoint]);const complete=()=>{const a=JSON.parse(localStorage.getItem('completed_steps')||'[]');if(!a.includes(step))localStorage.setItem('completed_steps',JSON.stringify([...a,step]))};async function submit(e:FormEvent){e.preventDefault();setLoading(true);setError('');try{const data=await apiFetch<any>(endpoint,{method:'POST',body:JSON.stringify(form)});setResult(data);if(listEndpoint){const x=await apiFetch<any>(listEndpoint);setItems(Array.isArray(x)?x:(x.items||x.jobs||x.applications||x.rows||[]))}}catch(x){setError(x instanceof Error?x.message:'Request failed')}finally{setLoading(false)}}return <section className="mx-auto max-w-5xl"><p className="text-xs font-bold uppercase tracking-[.25em] text-cyan-400">{eyebrow}</p><h2 className="mt-2 text-4xl font-bold tracking-tight text-white">{title}</h2><div className="mt-8 grid gap-6 lg:grid-cols-[minmax(0,1fr)_1fr]"><form onSubmit={submit} className="rounded-3xl border border-slate-800 bg-slate-900 p-6"><div className="space-y-4">{fields.map(f=><label key={f.name} className="block text-sm text-slate-300">{f.label}{f.optional&&<span className="ml-2 text-slate-600">optional</span>}<input required={!f.optional} type={f.type||'text'} value={form[f.name]||''} onChange={e=>setForm({...form,[f.name]:e.target.value})} className="field mt-2"/></label>)}<button disabled={loading} className="btn-primary w-full">{loading?'Working…':actionLabel}</button>{error&&<p className="text-sm text-rose-400">{error}</p>}</div></form><div className="space-y-3">{items.length?items.map((x,i)=><article key={i} className="rounded-2xl border border-slate-800 bg-slate-900 p-5"><h3 className="font-semibold text-white">{x.title||x.job_title||x.name||`Item ${i+1}`}</h3><p className="mt-1 text-sm text-slate-400">{x.company||x.location||x.status||x.rank_verdict||''}</p>{x.rank_score!=null&&<div className="mt-3 h-2 rounded bg-slate-800"><div className="h-2 rounded bg-cyan-400" style={{width:`${x.rank_score}%`}}/></div>}</article>):<div className="rounded-3xl border border-dashed border-slate-800 p-8 text-slate-500">Results will appear here.</div>}{result&&<pre className="max-h-72 overflow-auto rounded-2xl bg-slate-900 p-4 text-xs text-slate-300">{JSON.stringify(result,null,2)}</pre>}<div className="flex gap-3"><button onClick={()=>{complete();if(next)router.push(next)}} className="btn-secondary">{next?'Continue':'Mark as complete'}</button></div></div></div></section>}
