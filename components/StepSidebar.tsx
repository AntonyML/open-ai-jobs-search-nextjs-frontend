'use client'
import Link from 'next/link'
const steps = [['Providers','Config LLM','/providers'],['Setup','Perfil','/setup'],['Scrape','Buscar jobs','/scrape'],['Rank','Evaluar fit','/rank'],['Apply','CV + carta','/apply'],['Interview','Prep','/interview'],['Outcome','Tracker','/outcome']]
export default function StepSidebar({ currentStep, completedSteps }: { currentStep:number; completedSteps:number[] }) {
 return <aside className="w-full border-b border-slate-800 bg-slate-950 p-5 md:min-h-screen md:w-72 md:border-b-0 md:border-r"><div className="mb-8"><p className="text-xs font-semibold uppercase tracking-[.25em] text-cyan-400">Career OS</p><h1 className="mt-2 text-xl font-bold text-white">Job pipeline</h1></div><nav className="space-y-2">{steps.map(([label,sub,href],i)=>{const done=completedSteps.includes(i), active=i===currentStep, enabled=done||i<=currentStep; return enabled?<Link key={href} href={href} className={`flex items-center gap-3 rounded-xl p-3 transition ${active?'bg-cyan-400 text-slate-950':'text-slate-300 hover:bg-slate-900'}`}><span className={`flex h-7 w-7 items-center justify-center rounded-full border text-xs font-bold ${done?'border-emerald-400 text-emerald-400':active?'border-slate-950':'border-slate-600'}`}>{done?'✓':i+1}</span><span><span className="block text-sm font-semibold">{label}</span><span className={`block text-xs ${active?'text-slate-700':'text-slate-500'}`}>{sub}</span></span></Link>:<div key={href} className="flex items-center gap-3 rounded-xl p-3 text-slate-600"><span className="flex h-7 w-7 items-center justify-center rounded-full border border-slate-800 text-xs">{i+1}</span><span><span className="block text-sm">{label}</span><span className="block text-xs">{sub}</span></span></div>})}</nav></aside>
}
