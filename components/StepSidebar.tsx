'use client'
import Link from 'next/link'
const steps = [['Providers', 'AI provider', '/providers'], ['Setup', 'Your profile', '/setup'], ['Scrape', 'Find jobs', '/scrape'], ['Rank', 'Evaluate fit', '/rank'], ['Apply', 'CV + letter', '/apply'], ['Interview', 'Prepare', '/interview'], ['Outcome', 'Track progress', '/outcome']]
export default function StepSidebar({ currentStep, completedSteps }: { currentStep: number; completedSteps: number[] }) {
  return <aside className="w-full border-b bg-white px-5 py-5 md:min-h-screen md:w-72 md:border-b-0 md:border-r md:px-6 md:py-8"><div className="mb-8"><p className="text-xs font-semibold uppercase tracking-[.18em] text-[#707070]">Career OS</p><h1 className="mt-2 text-xl font-semibold tracking-tight text-[#1d1d1f]">Job pipeline</h1></div><nav className="space-y-1">{steps.map(([label, sub, href], i) => { const done = completedSteps.includes(i), active = i === currentStep, enabled = done || i <= currentStep; return enabled ? <Link key={href} href={href} className={`flex items-center gap-3 rounded-lg px-3 py-3 transition ${active ? 'bg-[#f4f8fb] text-[#1d1d1f]' : 'text-[#707070] hover:bg-[#f5f5f7]'}`}><span className={`flex h-7 w-7 items-center justify-center rounded-full border text-xs ${done ? 'border-[#2997ff] text-[#0066cc]' : active ? 'border-[#1d1d1f]' : 'border-[#d2d2d7]'}`}>{done ? '✓' : i + 1}</span><span><span className="block text-sm font-semibold">{label}</span><span className="block text-xs text-[#858585]">{sub}</span></span></Link> : <div key={href} className="flex items-center gap-3 rounded-lg px-3 py-3 text-[#b0b0b0]"><span className="flex h-7 w-7 items-center justify-center rounded-full border border-[#e2e2e5] text-xs">{i + 1}</span><span><span className="block text-sm">{label}</span><span className="block text-xs">{sub}</span></span></div> })}</nav></aside>
}
