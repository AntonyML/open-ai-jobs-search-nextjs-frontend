'use client'
import { FormEvent, useState } from 'react'; import Link from 'next/link'; import { useRouter } from 'next/navigation'; import { setToken } from '@/lib/auth'
export default function Login(){const [email,setEmail]=useState(''),[password,setPassword]=useState(''),[error,setError]=useState(''),router=useRouter(); async function submit(e:FormEvent){e.preventDefault();setError('');try{const body=new URLSearchParams({username:email,password});const r=await fetch(`${process.env.NEXT_PUBLIC_API_URL??'http://localhost:8000'}/api/v1/auth/login`,{method:'POST',headers:{'Content-Type':'application/x-www-form-urlencoded'},body});if(!r.ok)throw Error(await r.text());setToken((await r.json()).access_token);router.push('/providers')}catch(x){setError(x instanceof Error?x.message:'Unable to sign in')}}return <main className="flex min-h-screen items-center justify-center bg-slate-950 p-6"><form onSubmit={submit} className="w-full max-w-md rounded-3xl border border-slate-800 bg-slate-900 p-8 shadow-2xl"><p className="text-xs font-bold uppercase tracking-[.25em] text-cyan-400">Career OS</p><h1 className="mt-3 text-3xl font-bold text-white">Welcome back</h1><p className="mt-2 text-slate-400">Sign in to continue your job search.</p><div className="mt-8 space-y-4"><input required type="email" placeholder="Email" value={email} onChange={e=>setEmail(e.target.value)} className="field"/><input required type="password" placeholder="Password" value={password} onChange={e=>setPassword(e.target.value)} className="field"/>{error&&<p className="text-sm text-rose-400">{error}</p>}<button className="btn-primary w-full">Sign in</button></div><p className="mt-6 text-center text-sm text-slate-400">No account? <Link className="text-cyan-400" href="/register">Create one</Link></p></form></main>}
