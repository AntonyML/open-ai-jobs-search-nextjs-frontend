'use client'
import { FormEvent,useState } from 'react'; import Link from 'next/link'; import { useRouter } from 'next/navigation'; import { apiFetch } from '@/lib/api'
export default function Register(){const [form,setForm]=useState({full_name:'',email:'',password:''}),[error,setError]=useState(''),router=useRouter();const set=(k:string,v:string)=>setForm({...form,[k]:v});async function submit(e:FormEvent){e.preventDefault();try{await apiFetch('/api/v1/auth/register',{method:'POST',body:JSON.stringify(form)});router.push('/login')}catch(x){setError(x instanceof Error?x.message:'Registration failed')}}return <main className="flex min-h-screen items-center justify-center bg-slate-950 p-6"><form onSubmit={submit} className="w-full max-w-md rounded-3xl border border-slate-800 bg-slate-900 p-8"><p className="text-xs font-bold uppercase tracking-[.25em] text-cyan-400">Career OS</p><h1 className="mt-3 text-3xl font-bold text-white">Create your profile</h1><div className="mt-8 space-y-4">{[['full_name','Full name','text'],['email','Email','email'],['password','Password','password']].map(([k,p,t])=><input key={k} required type={t} placeholder={p} value={form[k as keyof typeof form]} onChange={e=>set(k,e.target.value)} className="field"/>)}{error&&<p className="text-sm text-rose-400">{error}</p>}<button className="btn-primary w-full">Register</button></div><p className="mt-6 text-center text-sm text-slate-400">Already registered? <Link className="text-cyan-400" href="/login">Sign in</Link></p></form></main>}
